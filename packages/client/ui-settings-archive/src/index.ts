/** Host loader entry for the archived-sessions browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the archived-sessions page. */
export function apply(): void {}

/** Host loader entry for the plugin-installer browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin-installer tab. */
export function apply(): void {}

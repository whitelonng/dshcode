/** Host loader entry for the plugin-control browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the plugin-control tab. */
export function apply(): void {}
